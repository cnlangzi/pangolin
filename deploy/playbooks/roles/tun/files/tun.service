[Unit]
Description=Pangolin Tunnel (tun)
After=network.target

[Service]
Type=simple
User=pangolin
Group=pangolin
WorkingDirectory=/opt/pangolin
# tun reads its config from /opt/pangolin/tun.yml (a single YAML file
# managed by Ansible — see roles/tun/templates/tun.yml.j2). Override
# the path with `--config` if needed.
ExecStart=/opt/pangolin/tun --config /opt/pangolin/tun.yml
ExecReload=/bin/kill -SIGUSR2 $MAINPID
Restart=on-failure
RestartSec=5s
LimitNOFILE=65536
KillMode=mixed
KillSignal=SIGTERM
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
