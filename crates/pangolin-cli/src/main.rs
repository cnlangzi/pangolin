// Pangolin CLI — placeholder. Real implementation: admin ops
// (create/list/delete site, domain, tun, token, cert).

fn main() {
    eprintln!("pangolin: not yet implemented — see README.md design");
    std::process::exit(1);
}
