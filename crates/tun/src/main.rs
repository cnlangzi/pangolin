// Pangolin tunnel node (tun) — entry point placeholder.
// Connects to ngx via WebSocket, validates token + name,
// receives domain list, proxies HTTP traffic to local services.

fn main() {
    eprintln!("tun: not yet implemented — see README.md design");
    std::process::exit(1);
}
