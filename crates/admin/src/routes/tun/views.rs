//! Tunnels HTMX partials.
//!
//! No HTMX endpoints for tun currently — the list page renders its table
//! inline rather than via an HTMX swap, and create / update / delete all
//! do a full page navigation. Kept for forward compatibility.

// Intentionally empty for now.
