//! Sites HTMX partials.
//!
//! The sites resource currently doesn't expose any `/api/sites/...`
//! HTMX endpoints — the list page renders its table inline rather than
//! via an HTMX swap, and create / update / delete all do a full page
//! navigation. This file is kept for forward compatibility (a future
//! issue may add a paginated `/api/sites/table` view, for example).

// Intentionally empty for now.
